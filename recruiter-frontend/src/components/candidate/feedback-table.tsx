import type { KitQuestion, SheetRead } from "@/hooks/use-interview-kit";

interface Props {
  questions: KitQuestion[];
  sheets: SheetRead[];
}

// Stub — Task 11 replaces this with a table comparing every interviewer's
// answers, ratings and verdict side by side.
export function FeedbackTable(_props: Props) {
  return null;
}
